apiVersion: v1
kind: Service
metadata:
  name: geoip-http-svc
  namespace: osg-services
spec:
  selector:
    k8s-app: geoip-http
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
